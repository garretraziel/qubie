extends base_user

block content
    if documents.length > 0
        ul
            each document in documents
                li: a(href="/user/"+document.id)= document.name
    else
        p Nutin.